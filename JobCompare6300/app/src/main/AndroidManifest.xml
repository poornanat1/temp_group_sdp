<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JobCompare6300"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AdjustSettings"
            android:label="Adjust comparison settings"
            android:exported="true">
        </activity>
        <activity
            android:name=".CurrentJob"
            android:label="Current job"
            android:exported="true">
        </activity>
        <activity
            android:name=".CompareJobs"
            android:label="Compare jobs"
            android:exported="true">
        </activity>
        <activity
            android:name=".CompareResults"
            android:label="Compare results"
            android:exported="true">
        </activity>
        <activity
            android:name=".NewOffer"
            android:label="New offer"
            android:exported="true">
        </activity>
    </application>

</manifest>